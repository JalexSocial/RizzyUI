
@page "/components/chart-axis-border"
@namespace RizzyUI.Pages
@using RizzyUI
@using RizzyUI.Charts
@inherits RzComponent

<PageTitle>Chart Axis Border</PageTitle>

<RzQuickReferenceContainer>
    <RzArticle ProseWidth="ProseWidth.UltraWide">
        <SideContent>
            <RzQuickReference />
        </SideContent>
        <MainContent>
            <RzBreadcrumb class="mb-4 not-prose">
                <BreadcrumbList>
                    <BreadcrumbItem>
                        <BreadcrumbLink Href="/" hx-boost="true" hx-select="#content" hx-target="#content" hx-swap="outerHTML">Docs</BreadcrumbLink>
                    </BreadcrumbItem>
                    <BreadcrumbSeparator />
                    <BreadcrumbItem>
                        <BreadcrumbLink Href="/components" hx-boost="true" hx-select="#content" hx-target="#content" hx-swap="outerHTML">Components</BreadcrumbLink>
                    </BreadcrumbItem>
                    <BreadcrumbSeparator />
                    <BreadcrumbItem>
                        <BreadcrumbPage>Axis Border</BreadcrumbPage>
                    </BreadcrumbItem>
                </BreadcrumbList>
            </RzBreadcrumb>

            <RzHeading Level="HeadingLevel.H1" QuickReferenceTitle="Axis Border" class="scroll-mt-20">Axis Border</RzHeading>

            <section>
                <RzMarkdown>
&gt; **Reference [External Link](https://www.chartjs.org/docs/latest/axes/styling.html#border-configuration)**
&gt;
&gt; It defines options for the border that run perpendicular to the axis.
&gt;
&gt; Namespace: ```options.scales[scaleId].border```
	
```csharp
.Options(o => o
    .Scales(s => s
		.ScaleId("y")
        .BeginAtZero(true)
        .Border(b => b
            .Color("#666")
            .Width(2))))
```

## Display
If true, draw a border at the edge between the axis and the chart area. Default ```true```
```csharp
.Display(false)
```

## Color
The color of the border line.
[External Link](https://www.chartjs.org/docs/latest/general/colors.html)
```csharp
.Color("#666")
```

## Width
The width of the border line. Default ```1```
```csharp
.Width(2)
```

## Dash
Length and spacing of dashes on grid lines. Default ```[]```
[External Link](https://developer.mozilla.org/en-US/docs/Web/API/CanvasRenderingContext2D/setLineDash)
```csharp
.Dash(1, 1, 2)
```

## DashOffset
Offset for line dashes. Default ```0.0```
[External Link](https://developer.mozilla.org/en-US/docs/Web/API/CanvasRenderingContext2D/lineDashOffset)
```csharp
.DashOffset(0.2)
```

## Z
z-index of the border layer. Values less than equal ```0``` are drawn under datasets, grater than ```0``` on top. Default ```0```
```csharp
.Z(-1)
```
                </RzMarkdown>
            </section>
        </MainContent>
    </RzArticle>
</RzQuickReferenceContainer>