
@page "/components/chart-font"
@namespace RizzyUI.Pages
@using RizzyUI
@using RizzyUI.Charts
@inherits RzComponent

<PageTitle>Chart Font Configuration</PageTitle>

<RzQuickReferenceContainer>
    <RzArticle ProseWidth="ProseWidth.UltraWide">
        <SideContent>
            <RzQuickReference />
        </SideContent>
        <MainContent>
            <RzBreadcrumb class="mb-4 not-prose">
                <BreadcrumbList>
                    <BreadcrumbItem>
                        <BreadcrumbLink Href="/" hx-boost="true" hx-select="#content" hx-target="#content" hx-swap="outerHTML">Docs</BreadcrumbLink>
                    </BreadcrumbItem>
                    <BreadcrumbSeparator />
                    <BreadcrumbItem>
                        <BreadcrumbLink Href="/components" hx-boost="true" hx-select="#content" hx-target="#content" hx-swap="outerHTML">Components</BreadcrumbLink>
                    </BreadcrumbItem>
                    <BreadcrumbSeparator />
                    <BreadcrumbItem>
                        <BreadcrumbPage>Font Configuration</BreadcrumbPage>
                    </BreadcrumbItem>
                </BreadcrumbList>
            </RzBreadcrumb>

            <RzHeading Level="HeadingLevel.H1" QuickReferenceTitle="Font Configuration" class="scroll-mt-20">Font Configuration</RzHeading>

            <section>
                <RzMarkdown>
&gt; **Reference [External Link](https://www.chartjs.org/docs/latest/general/fonts.html)**
&gt;
&gt; There are special global settings that can change all the fonts on the chart. These options are in ```Chart.defaults.font```. 
&gt; The global font settings only apply when more specific options are not included in the config.
	
```csharp
.Font(f => f.Family("Helvetica").Size(10))
```

## Family
Default font family for all text, follows CSS font-family options.
```csharp
.Family("Helvetica")
```

## Size
Default font size (in px) for text. Does not apply to radialLinear scale point labels. Default ```12```
```csharp
.Size(15)
```

## Style
Default font style. Does not apply to tooltip title or footer.
Does not apply to chart title. Follows CSS font-style options (i.e. normal, italic, oblique, initial, inherit).
```csharp
.Style("normal")
```

## Weight
Set a weight. Default font weight ```boldness```.
[External Link](https://developer.mozilla.org/en-US/docs/Web/CSS/font-weight)
```csharp
.Weight(FontWeight.Bold)
```
Or, set a number.
```csharp
.Weight(100)
```

## LineHeight
Height of an individual line of text.
[External Link](https://developer.mozilla.org/en-US/docs/Web/CSS/line-height)
```csharp
.LineHeight("3em")
```
Or, use double. Default ```1.2```
```csharp
.LineHeight(1.3)
```
                </RzMarkdown>
            </section>
        </MainContent>
    </RzArticle>
</RzQuickReferenceContainer>