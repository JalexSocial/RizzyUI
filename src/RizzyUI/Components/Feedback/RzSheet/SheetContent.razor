@namespace RizzyUI
@using Blazicons
@inherits RzComponent

<template x-teleport="body">
	<div x-show="open" x-cloak>
		<div class="@Theme.RzSheet.Overlay" x-on:click="close" x-bind:data-state="state"
		     x-show="open"
		     x-transition:enter="ease-in-out duration-500"
		     x-transition:enter-start="opacity-0"
		     x-transition:enter-end="opacity-100"
		     x-transition:leave="ease-in-out duration-300"
		     x-transition:leave-start="opacity-100"
		     x-transition:leave-end="opacity-0"></div>
		<div @attributes="AdditionalAttributes"
		     id="@Id"
		     class="@RootClass()"
		     data-slot="sheet-content"
		     x-show="open"
		     x-transition:enter="ease-in-out duration-500"
		     x-transition:enter-start="opacity-0"
		     x-transition:enter-end="opacity-100"
		     x-transition:leave="ease-in-out duration-300"
		     x-transition:leave-start="opacity-100"
		     x-transition:leave-end="opacity-0"		     
		     x-trap.inert="open"
		     x-bind:data-state="state">
			@ChildContent
			<button type="button" class="@Theme.RzSheet.CloseButton" x-on:click="close">
				<Blazicon Svg="Lucide.X" class="@Theme.RzSheet.CloseButtonIcon" />
				<span class="sr-only">@Localizer["RzSheet.CloseButtonText"]</span>
			</button>
		</div>
	</div>
</template>