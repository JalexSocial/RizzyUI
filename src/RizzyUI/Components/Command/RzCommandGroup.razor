
@namespace RizzyUI
@inherits RzComponent<RzCommandGroup.Slots>

<CascadingValue Value="this" IsFixed="true">
    @* This component now only serves as a logical container for items *@
    @* It does not render a DOM element itself, the list renderer handles grouping *@
    @ChildContent
</CascadingValue>