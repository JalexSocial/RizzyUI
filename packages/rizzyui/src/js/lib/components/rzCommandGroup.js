
// packages/rizzyui/src/js/lib/components/rzCommandGroup.js
export default function(Alpine) {
    Alpine.data('rzCommandGroup', () => ({
        // This component is now primarily a marker for RzCommandItem.
        // The rendering logic is centralized in rzCommandList.
        // This file is kept for potential future group-specific logic.
    }));
}